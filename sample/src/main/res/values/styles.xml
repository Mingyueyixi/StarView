<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <!--TabLayouts样式：字体小写-->
    <style name="TabLayout_lowercase" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/TextAppearance.Design.TabCaps</item>
    </style>
<!--字体小写样式-->
    <style name="TextAppearance.Design.TabCaps" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>
</resources>
